[package]
name = "clac-rs"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
ahash = "0.8.12"
clap = { version = "4.6.0", features = ["derive"] }

[build-dependencies]
vergen-git2 = {version = "9.1.0", features = ["build"]}
